# BendCAD replacement ledger

Operation-by-operation record of what was mined from `legacy/aethalgard/`
(immutable reference), what Bend code replaces it, and what remains open.
One row per replaced capability; C00 rows first.

Columns: operation | legacy source mined | intended Bend implementation |
law obligations | fixtures | limitations.

## C00 — contracts and failure semantics

| Operation | Legacy source | Bend implementation | Law obligations | Fixtures | Limitations |
|---|---|---|---|---|---|
| Structured failure taxonomy | `packages/geometry-contracts/src/kernel-error-registry.ts:31-93` (closed wire-code registry; `INVALID_REQUEST`, `CANCELLED`, `UNSUPPORTED_OPERATION`, …) and `kernel-protocol-core.ts:1667-1685` (`kernelErrorSchema` enum) | `src/c00/types.bend`: `FailKind` (exactly §8's six) + `Outcome` (`Ok`/`Incomplete`/`Failed`) | `taxonomy` (all six render); per-arm routing laws; contract L2 | `tests/c00/neg.bend` (16 checks assert kind + zero publication) | `F_nonconverged` triggerless until C05; no `retryable`/`agentHint` side table (TS-deriveable later) |
| Attributed failures | `kernel-protocol-core.ts:1689-1713` (a TIMEOUT MUST carry `operationId` + `details.timeoutMs`, enforced at the boundary) | `src/c00/eval.bend`: every `Outcome` arm carries `op: U64` (`op_id` of the request) | `failed_attributed`, `incomplete_attributed`, `empty_attributed`; contract L7 | `pos-empty-attributed`; `neg-fuel-*` checks assert carried id | Attribution is the content id, not a UUID lineage chain (C12) |
| Deterministic op identity | `packages/geometry-contracts/src/operation-hash.ts:187-205` (cumulative content hash) + `:34-39` (`excludedKeys`: execution inputs excluded) + `:47-54` (`jcsNumber` fails closed on nonfinite) | `src/c00/eval.bend`: `op_id` (FNV-1a over op/unit/params/rev/tolerances; fuel/bytes/cancel excluded) | `opid_excludes_fuel/cancel/bytes` (general); contract L6 | `pos-opid-*` (determinism, content/unit sensitivity, execution blindness) | FNV-1a identity, not a collision-resistant commitment; no cumulative prefix chain yet |
| Nonfinite input rejection | `operation-hash.ts:47-54` (`jcsNumber` throws on nonfinite — fail closed, never `null`) | `src/c00/types.bend`: `is_finite` via fork `F64.class < 2`, applied to all 4 param slots + all 5 tolerance entries at the boundary | `finite_accepts/rejects`, `nan_rejected`, `inftol_rejected`; contract L3 | `neg-nan-param`, `neg-inf-param`, `neg-ninf-unused-slot`, `neg-inf-tol` | Rejection only; no NaN-boxing or payload propagation semantics |
| Budget admission atomicity | `packages/geometry-contracts/src/mesh-budget.ts:116-121` (rejected admission leaves the accumulator untouched) + `:139-149` (ordered ceilings table, first crossing wins) | `src/c00/eval.bend`: pure `run` — `Failed`/`Incomplete` return the input `Model`; only `publish` builds the next revision | Per-arm `*_keeps_model` laws; `cancel_atomic` (general); contract L1/L2 | All `neg-*` assert `model_equal(outcome, pre)` | C00 budget is fuel + bytes; mesh ceilings stay in the pilot until tessellation (C10) |
| Cooperative cancellation | `native/kernel-host/src/cancel.hpp:1-30` (`Cancelled` + `UserBreak` poll of an atomic flag) | `src/c00/eval.bend`: `cancelled` flag sampled FIRST in the pipeline order, before any content inspection | `cancel_atomic`, `cancel_explicit`; contract L1 | `neg-cancelled`, `neg-cancel-beats-malformed` (order pin) | Single sample per request; no mid-evaluation polling points until ops grow past O(1) stages |
| Wall-clock budgets | `kernel-protocol-core.ts:58-90` (30 s default / 120 s export caps + 15 s escalation margin; request deadline reconciled ABOVE the op cap) | NOT in the evaluator: `Budget` is fuel + bytes only; time stays supervisor-side per the mined reconciliation design | `fuel0/1_*` laws pin the fuel half; contract §10 records the split | `neg-fuel-zero/one`, `neg-fuel-one-reports-invalid` | Supervisor + clock enforcement unimplemented (C13 surface) |
| Unit handling | `packages/geometry-contracts/src/tessellation-quality.ts:25-33` (linear mm and angular rad are SEPARATE named fields, each finite/positive) + `:54-60` (manufacturing preset pins both) | `src/c00/types.bend`: `LenUnit` tag + `to_mm` as the SOLE normalization boundary (mm authoritative); `Tol{lin, ang, …}` keeps linear/angular budgets separate per the mined precedent | `unit_cm/in/m` (python-pinned bit patterns); tolerance validation in `inftol_rejected`, `fuel1_reports_invalid` | `pos-len-m/cm/inch`; `neg-zero-tol`, `neg-inf-tol` | Length units only; angular/thermal/electrical units with their domains |
| Generational identity (seed) | `native/kernel-host/src/operation_timeout.hpp:107` + `operation_timeout.cpp:79-84` (generation-guarded waits detect stale epochs); `packages/geometry-contracts/src/refs-core.ts:28-52` (tokens name op/output/role/ordinal — content, never addresses) | `src/c00/types.bend`: `Model{rev, count, sum}` + request `rev`; publish advances `rev+1` atomically | `len_advances_rev/count`, `len_checksum`; contract L8 | `pos-len-*`, `pos-isect-overlap` assert `rev+1`/`count+1` | Full generational topology refs arrive in C04; no selector language yet |
| Empty-result semantics | `kernel-protocol-core.ts:869` (`missing` vs `failed` are distinct statuses — absence of result ≠ failure) | `src/c00/types.bend`: `S_empty` is `Ok` with the model unchanged | `empty_is_success`, `empty_keeps_model`; contract L4 | `pos-isect-empty`, `pos-isect-touch` (overlap boundary) | Scalar-interval emptiness only; topological emptiness in C07/C08 |

## C01 — mathematical foundation

| Operation | Legacy source | Bend implementation | Law obligations | Fixtures | Limitations |
|---|---|---|---|---|---|
| Placement frame orthonormalization | `native/kernel-host/src/geometry.cpp:176-194` (`PlacementFrame`: normalize z, project x, reject magnitude < 1e-9 absolute, reject parallel) | `src/c01/ops.bend`: `frame_basis` (Gram-Schmidt) + `mk_frame`/`mk_rigid` + `m3_is_rot` (rows unit ≤ 1e-12, orthogonal ≤ 1e-12, det within 1e-9 of +1) | `frame_ident_ok`, `frame_basis_ok`, `frame_ortho_inv`, `frame_lefthand_inv`, `frame_basis_par_unc`; contract M5 | `pos-frame-basis-ok`, `pos-frame-world/local`, `neg-frame-*`, `neg-basis-*` | Parallel test is RELATIVE (remnant ≤ 1e-12·‖x‖), strictly stronger than legacy's absolute 1e-9; angle-based ctors absent (no qualified trig; C03) |
| Vector input validation | `native/kernel-host/src/geometry.cpp:150-154` (`JsonVector` throws `expected vector3` on malformed input) | `src/c01/types.bend`: `mk_v2/mk_v3/mk_pt/mk_dr/mk_m3` reject any nonfinite component as `invalid-input` | `entry_vec`, `entry_pdm`; contract M6 | `neg-nan-*`, `neg-inf-*`, `neg-snan-v3` (every entry path, every slot class) | No JSON layer in C01; the boundary is F64 slots (interchange is C12) |
| Axis/tangent non-zero guard | `native/kernel-host/src/geometry.cpp:3074-3078` (revolve axis `Magnitude() < 1e-9` throws) and `:1629-1631` (zero tangent skips wall construction) | `src/c01/ops.bend`: `dr_unit`/`v3_normalize`/`v2_normalize` (zero → `invalid-input`, never NaN) + `seg_dir` (coincident → `invalid-input`) | `unit_zero_inv`, `drunit_zero_inv`, `seg_coincident_inv`; contract M2 | `neg-unit-*`, `neg-dr-zero`, `neg-seg-coincident`, `pos-seg-dir`, `pos-unit-*` | Exact-zero test replaces the 1e-9 magnitude gate (scale-free); callers needing a length floor apply it application-side |
| Tangent frame lateral (cross + normalize) | `native/kernel-host/src/geometry.cpp:1632-1633` (`normal.Crossed(tangent)` then `Normalize()`) | `src/c01/ops.bend`: `v3_cross` (validated) feeding `v3_normalize`; `frame_basis` computes y = z × x the same way | `cross_pins`, `frame_basis_ok` | `pos-cross-123`, `pos-frame-basis-ok` | Cross/dot are naive-order + validate-and-fail (spurious intermediate → uncertainty, never a wrong value) |
| Axis coincidence (dot + distance) | `native/kernel-host/src/geometry.cpp:3144-3148` (`SameInfiniteAxis`: \|dot\| ≥ 1 − 1e-10 and `Distance` ≤ 1e-7) | `src/c01/ops.bend`: `v3_dot`, `p_dist`/`v3_dist` primitives with overflow arms | `dot32`; contract M1/M3 | `pos-dot-32`, `pos-dist-345`, `neg-dot-overflow`, `neg-dist-overflow` | The 1e-10/1e-7 application thresholds stay application-side; C01 ships exact primitives, stated ulp bounds, and structured overflow |
| Dihedral angle kernel (cross·t, dot) | `native/kernel-host/src/geometry_measures.cpp:315-316` (`atan2(cross·t, dot)` with empirical sign calibration `:317-326`) | `src/c01/ops.bend`: `v3_cross` + `v3_dot` (the two scalar inputs); no `atan2` in C01 | `cross_pins`, `dot32` | `pos-cross-123`, `pos-dot-32` | `atan2`/trig policy belongs to C03; the sign-calibration precedent is recorded here for C11 queries |
| Positive-dimension guard precedent | `native/kernel-host/src/geometry.cpp:222-224` (box `width/depth/height must be positive`) | Degenerate-input → `invalid-input` throughout C01 (zero normalize, coincident `seg_dir`, non-rotation specs) | `unit_zero_inv`, `seg_coincident_inv`, `frame_*_inv`; contract M2 | `neg-unit-*`, `neg-seg-*`, `neg-frame-*`, `neg-rigid-*` | Dimension validation itself arrives with C06 primitives |
| Robust norms (no legacy precedent) | None — legacy uses naive `gp_Vec.Magnitude`/`Dot`, which can spuriously overflow | `src/c01/ops.bend`: `v3_norm_raw`/`v2_norm_raw` scaled hypot (max-scale, ratios in [−1,1]); scaled adjugate `m3_inv` | `norm345`, `norm_zero`, `norm_dbl_axis`, `norm_sub`, `norm_1e300`, `norm_dbl_diag_unc`, `inv_*`; contract M1 | `pos-norm-*`, `pos-inv-248`, `neg-norm-overflow` | Deliberate improvement over legacy: spurious overflow impossible by construction; true overflow → `numerical-uncertainty` |

## C02 — robust predicates and exact-number support

| Operation | Legacy source | Bend implementation | Law obligations | Fixtures | Limitations |
|---|---|---|---|---|---|
| Orientation predicates (no legacy precedent) | None — legacy delegates orientation to OCCT (`TopoDS` orientation queries, `geometry.cpp:2338,2738,2750`) with tolerance-based classification, never exact predicates | `src/c02/ops.bend`: `orient2d`/`orient3d` (Shewchuk Stage-A filter with 2x+ margins + exact small-integer kernel in U64 sign-magnitude arithmetic) | `o2_ccw/cw/coll_zero/rep_zero/near_neg/unc`, `o3_neg/pos/coplanar_zero/unc`, `o2_flip`; contract P1/P2/P3 | `pos-o2-*`, `pos-o3-*`, `neg-o2/o3-*`, 825-case differential vs Fraction truth | Exact for small integers (§4 domains) else filter-or-uncertain; overflow/subnormal-floor scales abstain honestly |
| Incircle/insphere (no legacy precedent) | None — no in-circle/sphere test in the mined sources | `src/c02/ops.bend`: `incircle`/`insphere` (translated 3x3 shared core / 4x4 Laplace; same filter+exact structure; RAW determinant signs) | `ic_in/out/on_zero/unc`, `is_in/out/on_zero/unc`; contract P1/P2/P3 | `pos-ic/is-*`, `neg-ic/is-*`, 488-case differential vs Fraction truth | Raw signs, no orientation canonicalization; insphere nonzero exact wins covered by analysis+stress, laws pin the zero arm |
| Segment chaining orientation | `native/kernel-host/src/geometry.cpp:1757` (orient segments head-to-tail), `:3601-3689` (oriented vertex walks; OCCT may return either orientation `:3655`) | `src/c02/ops.bend`: `seg_seg` (4× orient2d + exact bbox/1D-overlap; EXACT whenever sub-orients decide; point-segments admitted) | `seg_proper/touch/disjoint/overlap/ctouch/point_on/unc`; contract P5 | `pos-seg-*`, `neg-seg-*`, 202-case differential vs independent parametric oracle | First sub-failure in o1..o4 order wins; near-degenerate non-integral inputs propagate uncertainty |
| Hole-entry classification scale precedent | `native/kernel-host/src/geometry.cpp:2126-2130` (classification at the claimed entry point is scale-sensitive) | `src/c02/ops.bend`: `tri_class` (o0 degeneracy gate + 3 cyclic edge orientations; scale-free exact signs, no epsilon) | `tri_in/out/bound/degen_inv/nan_inv`; contract P5/P6 | `pos-tri-*`, `neg-tri-*`, 202-case differential vs independent barycentric oracle | Degenerate triangle is `invalid-input`; boundary needs no bbox (zero + inside-sided forces on-segment) |
| Small-integer exactness (no legacy precedent) | None — legacy has no exact-fallback path | `src/c02/types.bend`: `sint_of` (truncation-roundtrip detection) + `SB` sign-magnitude arithmetic with documented U64 maxima | `sint_int/half/lim/big/neg`; contract P3 | `pos-sint-*`; every integer-domain stress case decided exactly | Domains bounded (2^26/2^12/2^8); general expansion arithmetic NOT implemented (C02 abstains instead) |

## C03 — curves, surfaces, and trimming

| Operation | Legacy source | Bend implementation | Law obligations | Fixtures | Limitations |
|---|---|---|---|---|---|
| Rational curves core (no legacy precedent) | None mined — legacy curve evaluation delegates to OCCT `Geom`/`GC` (tolerance-based, transcendental parametrization) | `src/c03/ops.bend`: trig-free rational core — line, quartic-map circle, cubic Bezier (left-fold Bernstein), conic `RQBez` + `w1^2-w0*w2` discriminant, `rq_quarter` | `wrap_*`, `nev_*`, `bez/bder/qeval/qder` pins, `wq_bits`; contract §4/§5 | `pos-beval-*`, `pos-ceval-*`, `pos-cseam`, `pos-qeval-*`, `pos-rquarter-eval`, 62-check independent oracle (bitwise + Fraction + Decimal + 2000-sample sweep) | No transcendental evaluation (POLICY §1 reserves `sin_pi`/`cos_pi`/`atan2`); single-step periodic wrap only |
| NURBS evaluation (no legacy precedent) | None mined — legacy uses OCCT `Geom_BSplineCurve` | `src/c03/ops.bend`: cubic clamped non-periodic NURBS — fuel-bounded validation walks, span clamp `[3,nk-5]`, fixed de Boor in homogeneous coords, `0/0:=0` at repeated knots | `nok_*`, `nev_t05/t0/t1/bez_equiv/fuel0/fuel5_exh`; contract §7 | `pos-nev-*`, `pos-bn8-nok-fuel`, `neg-nev-*`, single-span = Bernstein bitwise | Cubic clamped non-periodic only; no NURBS derivative, insertion, or surfaces; short fuel → `resource-exhausted` |
| Surfaces: planes, quadrics, bicubic patches (no legacy precedent) | None mined — legacy uses OCCT `Geom_Surface` | `src/c03/ops.bend`: plane (orthonormal, no divide), sphere/cylinder/cone (slope-k, double-cone + side condition)/torus implicits + gradients, tensor-Bernstein `BezP` with exact partials, closed-form line/plane/sphere projection | `peval_*`, `sphp_*`, `spole_*`, `bn10_*` hull inclusions; contract §6/§10 | `pos-peval-*`, `pos-pdu/pdv-flat`, `pos-sphp-*`, `neg-sph-far` (far-field abstention) | Coarse-but-certified enclosures; no general closest-point or intersection (C07); trim composition for planes only |
| Trims in parameter space (no legacy precedent) | None mined | `src/c03/types.bend`: `TSeg`/`TBez` in surface `(u,v)` from the outset; `plane_trim_pt` composes through `plane_eval` | trim eval/bbox laws; contract §6 | `pos-tse-*`, `pos-tbe-*`, `pos-trimpt-23`, `pos-tsbb/tbbb` | Fixed representation, never retrofitted; other-surface composition in C07/C08 |
| Bend-native gates (process, not geometry) | None — new engineering property | `docs/bend-native-gates.md`: permanent BN-1..BN-12 per-packet checklist (N/A forbidden; every gate PASS/FAIL with evidence at the tested commit) | Per-packet gate tables in contract §11 + receipt §BN | This receipt §BN (12/12 PASS with cited evidence); audit fixed 4 contract violations + 1 native-lane arity issue before commit | Gates are evidenced claims, not proofs; ulp bounds stated + pin-tested, not machine-proved |
| C03.1 hardening: evaluator-boundary validity + scaled sphere projection (no legacy precedent) | None — independent re-audit finding (raw-constructed values bypassed constructor checks; `r/n` underflowed at extreme scale) | `src/c03/types.bend`: centralized `dom/ln/cc/rq/sp/cy/co/to_valid` consumed by constructors AND every evaluator; `src/c03/ops.bend`: `sph_proj_q_raw` scaled `c + r*((m/M)/(n/M))`, subnormal `d.d` → uncertainty arm, explicit `span_at` shared-fuel invariant | `val_*`, `dmid_degen/dlen_flip/clamp_degen/split_degen_inv`, `leval/lder_zero_inv`, `lproj/ldist_tiny_unc`, `qeval/qder/qbb/qdisc` weight pins, `sphv/sphg/sphp/spole/cylv/cylg/cyck/cov/cog/coax/cock/torv/torg/tock` positivity pins, `sphp_faraxis/minr/farctr_ok`, `nok_wneg_inv`; contract §2/§3/§5/§6/§7/§9/G9 | `neg-g8` (28 checks), `pos-g7` (4 checks), C03.1 oracle re-verification (bitwise) | Re-pins `sphp_345` x by 1 ulp (correctly-rounded 0.6) and `dmid_ovf` onto a valid domain; universal numerical proofs still future work |

## C04 — boundary-representation topology

| Operation | Legacy source | Bend implementation | Law obligations | Fixtures | Limitations |
|---|---|---|---|---|---|
| Edge vs oriented use split | `native/kernel-host/src/geometry.cpp:3601-3689` (oriented vertex walks via `TopExp::Vertices(edge, first, last, CumOri=true)` + tolerance-chained connectivity) and `:3653-3656` ("OCCT may return either orientation for the same durable selector" — orientation is OCCT-internal state) | `src/c04/types.bend`: unoriented `Ed` (geometry + endpoints) vs `Ce` (edge handle + explicit `fwd` + trim in face `(u,v)`); orientation is DATA, never a kernel flag | `ev_*`, `eclosed/edegen_*`, `ce_valid_*`, `mk_ec/edg/ce_*`; contract §1/§3 | `pos-ec-ok/closed-ok`, `pos-ce-fwd/rev-ok`, `pos-lp-seam2-ok`, `neg-ec-*`, `neg-ce-*`, `neg-ed-degen-*` | Edge-use validation implemented (`brep_checked` stage C: twice-opposite; `ck_inv`/`ck_nonm`); vertex-link pinch deferred (contract §12) |
| Closed edges, seams, degenerate poles (no legacy precedent) | None mined — OCCT owns closed/seam/degenerate edge semantics internally (no mined seam/pole/coedge handling in `legacy/` sources or topology docs) | `src/c04/types.bend`: closed = `v0 == v1` with `EK_curve` (accepted); seam = two coedges, opposite `fwd`, on one loop; pole = `EK_degenerate` pinched to one vertex (`edge_valid` rejects the split degenerate) | `ev_closed/ev_degen/ev_degen_no`, `eclosed_yes/no`, `edegen_yes/no`, `mk_edg_ok/split`; contract §1 | `pos-ec-closed-ok`, `pos-ed-ok`, `pos-ev-*`, `neg-ed-degen-idx/gen` | Seam acceptance (`ck_sphere`/`ck_cyl`) + pole (`ck_pole`) implemented and exit-pinned; quadric trim 3D coincidence skipped (contract §12) |
| Loops, faces, shells, solids, cavities, compounds (no legacy precedent) | None mined — B-rep assembly delegates to OCCT `TopoDS` shapes throughout the mined sources | `src/c04/types.bend`: `Lp` (nonempty coedge list + kind), `Fa` (surface arm + loops + `same`), `Sh` (faces + outer/inner/open kind), `So` (outer + cavity list), `Cp` (solids + open sheets), `Brep` (7 explicit stores + top + stamp); cavity IS an `SK_inner` shell | `lp/fa/sh/so/cp_valid_*`, `tag_*/show_*`, `mk_lp/fa/sh/so/cp_*`, `mk_brep_*`; contract §1/§2/§3 | `pos-lp/fa/sh/so/cp-*`, `pos-mb-empty/full/stamp`, `neg-lp/fa/sh-*`, `neg-mb-*`, tet probe (4V/6E/12CE/4L/4F/1SH/1SO, Euler on observed lens; re-run fresh at close-out, byte-identical) | Both profiles implemented (`brep_checked` stages B–G); push admits dangling by design, diagnosed at stage B (`ck_dang`); cavity inside/disjoint deferred (contract §12) |
| Generational topology handles | `packages/geometry-contracts/src/refs-core.ts:28-52` (tokens name content, never addresses; C00 ledger seed) + `native/kernel-host/src/operation_timeout.hpp:107` (generation-guarded epochs) | `src/c04/types.bend`: `(idx, gen)` handles into explicit `Brep` lists; global `stamp+1` issue; `resolve` rejects stale as `invalid-input`; gen 0 = unissued; `issued/push/handle_of` protocol per entity | `mk_h*_idx/gen` (general over every idx/gen), `null_*`, `eq_*`, per-entity `len/at/res/hof/iss/push_*` (counts, head-free, stale/oob/unknown/empty/fuel arms, round-trips); contract §5/§6 | `pos-eq-*`, `pos-hof/iss-*`, `pos-psh-*`, `pos-at/res-*`, `neg-at/res/hof/iss-*`, `neg-len-*` | Stores append-only (no delete/update); staleness today means forged/cross-brep handles, not removal |
| Walk-code packing (process, not geometry) | None — new engineering property (C03 `code_pack` precedent) | `src/c04/types.bend`: `okd_pack` (`ok + 2*done`), `okd_all` over the seven store walks; `mk_brep` checks `done` before `ok` | `okd_*`, `allc_*`; contract §5 | `pos-okd-*`, `pos-allc-*`, `neg-mb-fuel-*`; oracle recomputes the full 16-pair table | Shared-fuel cap is per-store full (not divided); documented, not tuned |
| B-rep validity profiles (manifold-solid vs open-shell) | None mined — validation delegates to OCCT `BRepCheck`/`TopoDS` throughout the mined sources, with no portable rule set to lift | `src/c04/ops.bend`: `brep_checked` stages B–G (referential B, edge-use pairing C, loop closure + repeated-start proxy D, face-kind E, shell-kind + ownership F, embedding G) on fuel-capped full-cap walks; C04.1 adds outer-`SK_outer` + single-ownership (`sos_outer_kind_c`, `sos_own_c`, `cp_sh_own_c`); MASTER_PLAN §C04 exit fixtures | `ck_sphere/cyl/cav/open/pole` accepts + `ck_dang/inv/trimx/embx/nonm/selfx/open_solid/alias/outerkind/cavdup/reuse/cpshare/fuel` diagnoses + `hsh_nd/fo`, `so_shl`, `outer_kind`, `own`, `cpown`, `hlf`, `hfos` walk pins; contract §1/§3/§4 | `pos-ck-*` (5) + `neg-ck-*` (13) on all three lanes; oracle asserts all 18 exit lines | Vertex-link pinch + cavity inside/disjoint + face-orientation coherence deferred; quadric trim 3D coincidence skipped; repeated-start is a proxy, not general self-intersection (contract §12) |
